import * as React from 'react';

export interface Props {
    list: Array<String>;
  }

function Form({ list }: Props) {

    return (
        <div>

            test

        </div>
    );
}

export default Form;